on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - run: echo hazeng-dev
    - uses: ./hazeng-action
      with:
        minimum: '10'
        maximum: '20'
      id: task
    - run: echo "The random number is ${{ steps.task.outputs.result }}"
